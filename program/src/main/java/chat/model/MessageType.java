package main.java.chat.model;

/**
 * Enum representing the possible message types in the chat application.
 */
public enum MessageType {
    TEXT,
    IMAGE,
    FILE,
    AUDIO,
    VIDEO,
    LOCATION,
    SYSTEM
}